import { MakediscountPipe } from './makediscount.pipe';

describe('MakediscountPipe', () => {
  it('create an instance', () => {
    const pipe = new MakediscountPipe();
    expect(pipe).toBeTruthy();
  });
});
